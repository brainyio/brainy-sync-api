#!/usr/bin/env node
var requirejs = require('requirejs');

requirejs.config({
  name: 'brainy-sync-api',
  deps: ['main'],
  baseUrl: __dirname + '/..',
  paths: {
    main: __dirname + '/main',
    src: 'src'
  }
});